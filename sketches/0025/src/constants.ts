import packageData from '../package.json';

export const SKETCH_ID = packageData.name;
export const WIDTH = 1080;
export const HEIGHT = 1920;
export const DIV_WIDTH = WIDTH / 2;
export const DIV_HEIGHT = HEIGHT / 2;
export const RECORDING_FPS = 60;
export const ASPECT = WIDTH / HEIGHT;
